package com.nesvadba.tomas.cct.enums;

public enum ComponentProperty {
    SIZE, ROUND, NODE_COUNT, LEFT, RIGHT, UP, DOWN, CENTERX, CENTERY, G_HEIGHT, INTENSITY, HEIGHT, WIDTH, PERIMETER, ELONGATION, AVG_INTENSITY,
}
