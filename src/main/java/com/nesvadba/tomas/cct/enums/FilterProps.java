package com.nesvadba.tomas.cct.enums;

public enum FilterProps {
    SIZE_MAX, SIZE_MIN, HEIGHT_MIN, HEIGHT_MAX, INTENSITY_MIN, INTENSITY_MAX, ROUND_MIN, ROUND_MAX,
    LEFT,RIGHT,UP,BOTTOM,MAX,MIN
}
